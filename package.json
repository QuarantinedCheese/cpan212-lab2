{
  "name": "cpan212-lab2-starter",
  "version": "1.0.0",
  "description": "CPAN 212 Lab 2 starter: Tool Library REST API with Express 5",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "check": "node check.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
